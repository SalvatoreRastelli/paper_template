\documentclass[letterpaper]{article} % DO NOT CHANGE THIS
\usepackage[submission]{aaai2027}  % DO NOT CHANGE THIS
\usepackage[hyphens]{url}  % DO NOT CHANGE THIS
\usepackage{graphicx} % DO NOT CHANGE THIS
\urlstyle{rm} % DO NOT CHANGE THIS
\def\UrlFont{\rm}  % DO NOT CHANGE THIS
\usepackage{natbib}  % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\usepackage{caption} % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\frenchspacing  % DO NOT CHANGE THIS
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{ragged2e}
\usepackage{todonotes}
\usepackage{xspace}

\newtheorem{proposition}{Proposition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem{definition}{Definition}
\newtheorem{conjecture}{Conjecture}

\pdfinfo{
/TemplateVersion (2027.1)
}

\setcounter{secnumdepth}{2}

\newcommand{\merw}{EigenTree\xspace}
\newcommand{\merwft}{EigenTree-FT\xspace}
\newcolumntype{Y}{>{\RaggedRight\arraybackslash}X}

% \title{EigenTree: A Decentralized Communication Protocol Achieving Centralized Multi-Armed Bandit Results}
\title{EigenTree: a Centralizing Communication Protocol\\for Decentralized Multi-Agent Multi-Armed Bandit}
\author{
    Anonymous Submission
}
\affiliations{
    %
}

\begin{document}

\maketitle

\begin{abstract}
	Centralized learning achieves the best statistical performance by
	aggregating all observations, but such global coordination is unavailable
	in decentralized cooperative systems.
	We propose \merw, a decentralized communication protocol on undirected
	graphs that establishes, from purely local interactions, a coordinator
	among the agents.
	\merw uses eigenvector centrality to 1) choose the most central node as
	coordinator, and 2) construct a routing tree, rooted at the coordinator
	node, which spans the graph.
	Information flows according to this routing tree: from leaves to root
	during the aggregation phase, and vice versa during the broadcasting phase.
	We prove that, under this protocol, a bandit algorithm based on the
	coordinator's information state achieves the same statistical performance
	as a centralized instance, at the cost of $O(D)$ communication rounds, with
	$D$ the tree's diameter.
	The protocol can be used in various decentralized cooperative learning
	setups, of which we show two---regret minimization and best arm
	identification---where the method recovers centralized-like results in
	finite time.
	We further present a fault-tolerant version of the protocol, addressing the
	coordinator's structural weakness as a single point of failure.
\end{abstract}

\section{Introduction}

In cooperative multi-agent learning, no individual agent has access to the
global state of the system, but the best-performing strategy is usually the one
available to a centralized decision maker with a full view of everyone's
observations. This is the setting whenever $N$ agents connected by an
undirected graph $G = (V, E)$ must repeatedly act and learn from feedback while
exchanging only local messages with their graph neighbors. Existing approaches
close the gap to centralized performance either by symmetric averaging, where
every agent maintains its own estimate and no coordinator ever exists (e.g.,
Coop-UCB2 \citep{landgren2021distributed}), or by assuming a communication
model rich enough that any agent can reach any other in one step (e.g., the
all-to-all broadcast of \cite{hillel2013distributed}). 

We instead ask whether the communication graph $G$ itself can select which
agents should play a coordinating role. This motivates measuring an agent's
importance through a global notion of centrality. Building on this idea, we
propose \merw, a method to \emph{construct}, in a completely decentralized
way, a communication structure according to the eigenvector centrality induced
by $G$.

In \merw, a coordinator \emph{emerges} from the graph structure, identified
from purely local computation, with no agent ever seeing the global topology or
knowing $N$ in advance. The key idea is that eigenvector centrality, computed
via a fully decentralized gossip protocol \citep{jelasity2007asynchronous},
can be used to identify a ``most central'' node in $G$. One can then construct
a routing tree which spans $G$ via max-flooding, again only using local
information. Once the tree is in place, the hub acts as a de-facto central
coordinator: every agent routes its observations up toward the hub, which sees
the globally aggregated picture and broadcasts its decisions back to the other
nodes. We prove that this emergent coordinator is statistically equivalent to a
true centralized coordinator for any decision rule that depends only on
globally aggregated per-agent statistics, at the cost of a bounded
communication overhead determined by the tree's depth.

This last property is what makes \merw a \emph{protocol} rather than a single
algorithm: any decentralized cooperative-learning task whose decision rule
depends only on globally aggregated per-agent statistics can be run over the
same emergent hub-and-tree structure by supplying two application-specific
rules, namely 1) what each agent contributes locally and 2) what the hub
decides from the aggregate. We instantiate both rules for two standard
cooperative multi-armed bandit objectives, \emph{regret minimization} and
\emph{best arm identification} (BAI), and show in both cases that the
emergent coordinator recovers centralized-level performance.

The emergent-coordinator design has one structural weakness: the hub is a
single point of failure, and more generally, any node's failure disrupts the
part of the tree that routes through it. Hub failure is the more consequential
case, since it can stall the entire network; we show it can be tolerated with
zero communication overhead during normal operation, by designating, among the
hub's children, the most central one as a passive backup. This node already
mirrors the hub's state through the ordinary downlink broadcast. Non-hub
failure, which only disrupts a subtree, is repaired even more cheaply, via
precomputed swap edges.

\paragraph{Contributions.} (1) A fully decentralized protocol for building a
centralized communication structure: the hub and tree emerge from local
computations, requiring no privileged node, no global communication, and no
prior knowledge of $N$ or the graph structure. (2) A proof that this emergent
coordinator is statistically sufficient for any decision rule depending only on
globally aggregated statistics, instantiated for regret minimization and best
arm identification, both matching centralized performance up to an $O(D)$
communication overhead per round, where $D$ is the depth of the induced tree.
(3) \merwft, a passive-backup extension tolerating permanent failure of any
node, hub or non-hub, at zero or near-zero steady-state cost.

\section{Related Work}
\label{sec:related}

\paragraph{Cooperative multi-agent bandits.} Coop-UCB2
\citep{landgren2021distributed} uses doubly stochastic consensus weights to
diffuse observations across the graph; every agent plays an equivalent role and
no coordinator is assumed. % Mart\'inez-Rubio et al.\
\cite{martinez2019decentralized} introduce the total-pull clock we adopt to
index UCB confidence intervals by actual observations rather than wall-clock
rounds.
% Hillel et al.\ 
\cite{hillel2013distributed} study cooperative BAI
under all-to-all broadcast; we build directly on their Algorithm 3, replacing
the broadcast with hop-by-hop relay over the routing tree. In all three cases, the
communication model is fixed in advance (symmetric gossip or all-to-all);
\merw instead derives a communication structure, the hub and tree, from the
graph itself, and shows it is sufficient for the same class of decision rules.

\paragraph{Eigenvector centrality and decentralized computation.}
Eigenvector centrality can be studied in the context of Maximal Entropy Random
Walk \citep{burda2009localization}, where it appears in the associated
stationary distribution. This framing gives an information-theoretic meaning
for eigenvector centrality, and it is on the basis of this argument that we
justify its usage (see Sec.~\ref{sec:merw}).
In principle, computing the eigenvector centrality requires solving the
eigenvalue problem associated with the graph $G$;
\cite{jelasity2007asynchronous} give a gossip-based normalization for
distributed power iteration that avoids a global all-reduce, which we use
directly to compute the first eigenvector without any agent ever knowing $N$.

\paragraph{Fault tolerance in tree-structured coordination.} The passive-backup
pattern underlying \merwft is standard in distributed systems: OSPF's Backup
Designated Router \citep{rfc2328} and RSTP's Alternate Port \citep{ieee8021w}
both pre-identify a successor that promotes itself locally upon primary
failure. \cite{lamport2009vertical} formalize passive replication as a special
case of Paxos, establishing that a mirroring backup is at most one
synchronization round stale; \merwft applies this pattern to the
aggregation-tree setting, and additionally handles non-hub failure via the
swap-edge framework of \cite{nardelli2003}.

\paragraph{Minimum-diameter spanning trees.} Choosing the coordinator to
directly minimize the routing tree's diameter is a solved problem: the optimal
root is the graph's absolute center, and a shortest-path tree rooted there is a
minimum diameter spanning tree (MDST) \citep{hakimi1964,hassintamir1991}. This
has been made fully distributed
\citep{bui2013distributed,butelle1995selfstabilizing}, including a
self-stabilizing version that recovers after a fault. These methods are exact,
but the cost is structural: computing the absolute center requires every node
to hold all-pairs shortest-path information, i.e., full topological knowledge
of $G$, not just its own neighborhood: $O(N)$ per-node distance entries at
minimum, and $O(N^2)$ total network state \citep{bui2013distributed}. A
permanent node failure changes the graph itself, so the center may move, and
the construction (or, in the self-stabilizing variant, a full reconvergence
over the network) must run again, with no bounded, localized way to react to a
single failure. \merw makes the opposite trade-off: the hub is chosen from
local gossip alone, with no agent ever storing distances to more than its
immediate neighbors, and \merwft recovers from a single hub failure in a
number of rounds bounded by the tree depth $D$, using a precomputed backup,
rather than reconverging the whole network. Section~\ref{sec:mdst-gap}
quantifies what this costs in tree diameter.

\section{EigenTree: The Protocol}

\subsection{Hub Emergence and Routing}
\merw is based on the eigenvector centrality; we describe here how we obtain
it in a purely decentralized way, and how we use it to construct a routing tree
that spans the graph $G$.

Formally, let $A \in \{0,1\}^{N \times N}$ be the adjacency matrix of $G$. By
the Perron--Frobenius theorem \citep{hornjohnson2013}, $A$ has a unique
positive leading eigenvalue $\lambda_1$ with strictly positive eigenvector
$\psi$ such that $A\psi = \lambda_1 \psi$ and $\|\psi\|_2 = 1$.

No single agent has access to $A$, so $\psi$ is computed by the distributed
power iteration of \citep{jelasity2007asynchronous}. Each agent $i$ keeps a
local scalar $w_i$, initialized to 1, and repeats: (1) collect $w_j$ from every
neighbor and form the unnormalized update $\tilde w_i = \sum_{j\sim i} w_j$;
(2) compute the local log-growth rate $r_i = \log(\tilde w_i/w_i)$; (3) average
$r_i$ with random neighbors for $O(\log N)$ gossip rounds, so that every $r_i$
converges to the same network-wide estimate of $\log\lambda_1$ without any node
ever summing over all of $N$; (4) normalize locally, $w_i \leftarrow \tilde
w_i/\exp(r_i)$. This gossip step replaces the single global $\ell_2$-norm
renormalization an ordinary (non-distributed) power iteration would need, at
the cost of the extra gossip rounds. Importantly, only the \emph{ratio}
$\psi_j/\psi_i$ is needed for routing: agent $i$ simply checks which neighbor's
value is larger. Thus, the common unknown scale $\tilde\psi_i \approx
c\cdot\psi_i$ left behind by this normalization is never resolved and never
needs to be: no agent ever learns $N$ or $c$.

After power iteration, a short \emph{max-flooding} phase merges any local
maxima of $\tilde\psi$ into a single spanning tree. Each agent $i$ keeps a
flooded value $m_i$, initialized to $\tilde\psi_i$, and a pointer
$\mathrm{via}_i$ initialized to itself. Every round, $i$ broadcasts $m_i$ to
its neighbors; if some neighbor $j$ reports $m_j > m_i$, $i$ sets $m_i
\leftarrow m_j$ and $\mathrm{via}_i \leftarrow j$. Once a round passes in which
no agent updates its value, the flood has converged, purely a local
observation, with no agent needing to know $\mathrm{diam}(G)$ in advance. At
convergence, $m_i$ equals the global maximum $\tilde\psi_{h^\star}$ everywhere,
and $\mathrm{via}_i$ points one step closer to $h^\star$; any node whose own
$\tilde\psi_i$ was a local but not the global maximum re-routes toward
$\mathrm{via}_i$, while $h^\star$ itself, whose value never has a better
neighbor, keeps no parent and becomes the root. The result is a single directed
spanning tree $\mathcal{T}$ rooted at $h^\star = \arg\max_i \tilde\psi_i$,
converging in at most $\mathrm{diam}(G)$ rounds. Immediately afterward, each
node $i$ knows only its own parent $\mathrm{par}(i)$; its children, its depth
$d_i$, and the tree height $D$ are all discovered incrementally during the
protocol's first cycle below, not known in advance.

Complete proofs of every theorem, proposition, and corollary in this paper are
given in the supplementary material, using matching notation and statement
numbering; we give the proof of Theorem~\ref{thm:coordinator} here as well,
since it is the paper's central claim.

\begin{proposition}[Centralized aggregation and broadcast via routing]
\label{prop:aggbcast}
If each node $i$ holds a scalar $x_i$ and propagates it toward $h^\star$ one
	hop per round (each intermediate node forwarding the sum received), the hub
	holds $\sum_i x_i$ after $D$ rounds, where $D$ is the tree height.
	Symmetrically, a value broadcast from $h^\star$ one hop per round reaches
	every node after $D$ rounds. Neither direction requires any node to know
	$D$ in advance: the uplink terminates when the hub observes a round with no
	incoming packets, and each node infers its own depth $d_i$ from the hop
	count carried on the first downlink it receives.
\end{proposition}

% \subsection{Information flow perspective}
\subsection{A Path-Based Motivation for \merw}
\label{sec:merw}

We provide here a heuristic for choosing eigenvector centrality as our guiding metric.

In a federated setup, nodes share their states and perform updates. One can
think of the state information to be shared as a ``packet'' that undergoes a
random walk on the graph. Let us consider two nodes $i,j \in G$, with a packet
originating from $i$ and reaching $j$. For a path length $L \geq
\mathrm{dist}_G(i, j)$, there are generally many distinct paths joining $i$ and
$j$. We assume there is no reason for one specific path to be favored compared
to another: all admissible paths of a certain length $L$ which connect $i$ and
$j$ are thus equally likely. Crucially, this condition is \emph{non-local};
while an unbiased random walk prescribes all neighbors to be equally likely,
this process considers all long paths. The question then becomes: which node
$h\in G$ is most frequently visited under this path ensemble? That node will be
involved in the largest portion of message exchanges, and thus be a natural
candidate to be a hub.

The framing we just described is a Maximal Entropy Random Walk (MERW) on $G$
\citep{burda2009localization}. The MERW is the walk on $G$ whose transition
probabilities make every path of a given length between two fixed nodes equally
likely, unlike the standard random walk, which only weighs each single step by
degree and has no notion of the paths it is part of. Because MERW weighs entire
paths equally, a node's stationary probability under MERW reflects its
participation in long paths throughout the graph, not just how many neighbors
it has.

MERW's stationary distribution is proportional to the eigenvector centrality,
and this is the link between MERW and \merw. Computing the eigenvector
centrality allows one to identify the hub node $h\in G$ which is most
frequently traversed.

\subsection{Coordinator Sufficiency}

\begin{theorem}[Coordinator sufficiency]
\label{thm:coordinator}
Let $\mathcal{A}$ be any decision rule whose output at each step depends only on globally aggregated per-agent statistics $B = \sum_{i \in V} b_i$, where $b_i$ is agent $i$'s local contribution. If a coordinator receives the exact aggregate $B$ within $D$ rounds and broadcasts its decision within $D$ further rounds, with all agents synchronized via the depth signal carried on the broadcast, then the coordinator's information state at every decision point is identical to that of a centralized instance of $\mathcal{A}$, with an additive overhead of at most $2D$ time steps per decision round.
\end{theorem}

\begin{proof}
Fix any decision round $r$. By Proposition~\ref{prop:aggbcast}, after $D$ rounds the hub holds $B = \sum_{i\in V} b_i$ exactly, not an approximation, since every node's contribution is included by the uplink termination argument. Because $\mathcal{A}$'s decision at round $r$ depends only on $B$, the hub computes exactly the action a centralized instance of $\mathcal{A}$ would compute given the same per-agent contributions. By Proposition~\ref{prop:aggbcast} again, within a further $D$ rounds every agent holds the hub's decision and has derived its own depth $d_i$ from the hop count on this broadcast, so all agents can identify when the current cycle ends and begin the next round's contribution simultaneously, regardless of their depth in $\mathcal{T}$. Hence the sequence of aggregates and decisions seen across the whole run is identical, round for round, to what a centralized instance of $\mathcal{A}$ would see given the same sequence of per-agent contributions; the only difference is the $2D$ rounds of communication latency inserted between successive decisions.
\end{proof}

\merw runs in synchronous rounds. Every agent takes one action per discrete
time step. A pull, a forwarded packet, any outcome of a step, becomes visible
to its recipient only at the start of the next step.

Algorithm~\ref{alg:merw-generic} gives \merw as a single generic protocol.
After initialization, agents repeat a fixed cycle of four phases: Pull,
Uplink, Hub decision, Downlink. Three parts of this cycle are left open for
an application to fill in:
\begin{itemize}
\item $\textsc{Init}$
\item $\textsc{LocalPull}$
\item $\textsc{HubDecide}$
\end{itemize}
Together these three fix what each agent contributes locally and what the
hub decides from the aggregate $B$. Any decentralized learning task whose
decision rule fits Theorem~\ref{thm:coordinator} can run over the same
emergent tree this way, just by choosing a different $\textsc{Init}$,
$\textsc{LocalPull}$, and $\textsc{HubDecide}$. Table~\ref{tab:instantiations}
gives these three for regret minimization and best arm identification,
detailed in the two subsections below.

\begin{algorithm}[tb]
\caption{\merw (generic protocol)}
\label{alg:merw-generic}
\begin{algorithmic}[1]
\STATE \textbf{Require:} graph $G$; hooks $\textsc{Init}$, $\textsc{LocalPull}$, $\textsc{HubDecide}$; a stopping condition on $t$ or on a hub-computed flag $\mathrm{done}$
\STATE Compute $\tilde\psi$ via distributed power iteration; run max-flooding to obtain tree $\mathcal{T}$ rooted at $h^\star$
\STATE $t \leftarrow 0$; $\mathrm{done} \leftarrow \textbf{false}$; each node $i$ knows only $\mathrm{par}(i)$ so far
\FORALL{$i \in V$}
    \STATE $\sigma_i \leftarrow \textsc{Init}()$; $t \mathrel{+}= (\text{rounds used})$
\ENDFOR
\REPEAT
    \FORALL{$i \in V$}
        \STATE $(b_i, \sigma_i) \leftarrow \textsc{LocalPull}(\sigma_i)$; $t \mathrel{+}= (\text{rounds used})$
    \ENDFOR
    \STATE \emph{Uplink}: every node forwards $b_i$ toward $h^\star$ once it has heard from all its children; the hub absorbs packets until a round passes with none arriving, yielding $B = \sum_i b_i$ (Prop.~\ref{prop:aggbcast}); no node needs $D$ in advance
    \STATE $(m, \mathrm{done}) \leftarrow \textsc{HubDecide}(B)$; $t \mathrel{+}= 1$
    \STATE \emph{Downlink}: hub sends $(m, D, \mathrm{done}, \mathrm{hops}{=}0)$ to $\mathrm{ch}(h^\star)$; each node that receives $(m, D, \mathrm{done}, \mathrm{hops})$ sets its own depth $d_i \leftarrow \mathrm{hops}+1$, updates $\sigma_i$ from $m$, and forwards $\mathrm{hops}{+}1$ to its children; this is how every node first learns $D$ and $d_i$
\UNTIL{$\mathrm{done}$ \textbf{or} $t \geq T$}
\end{algorithmic}
\end{algorithm}

\begin{figure}[tb]
    \centering
    \includegraphics[width=0.95\columnwidth]{results/MERW_visualization/merw_tree_er_N20_p0.5.pdf}
    \caption{Eigenvector centrality and routing tree on an Erd\H{o}s--R\'{e}nyi graph ($N=20$, $p=0.5$). \emph{Left}: undirected graph, nodes colored by $\psi_i/\psi_{\max}$. \emph{Center}: routing subtree overlaid on the graph, hub ringed in gold. \emph{Right}: induced directed tree, hierarchical layout, arrows child $\to$ parent.}
    \label{fig:merw_viz_er}
\end{figure}

\subsection{\merw vs. Minimum Diameter Tree}
\label{sec:mdst-gap}

\merw sets its root at $h^\star$, the highest eigenvector centrality node.
This is in general \emph{not} the graph's absolute center, i.e., the root that
would minimize tree depth and give a minimum diameter spanning tree (MDST)
\citep{hakimi1964,hassintamir1991}. Thus the tree depth $D =
\mathrm{ecc}_G(h^\star)$, which sets the $O(D)$ communication overhead of
Theorem~\ref{thm:coordinator}, can exceed $D' = R(G)$, the depth an MDST would
achieve. We bound this gap in two regimes, first an unrestricted worst case and
then two conditions, one on degree and one on spectral expansion, under which
the gap is small.

\paragraph{Worst case.} Consider a graph formed by attaching a high-degree cluster
to the end of a long path: $h^\star$ settles inside the cluster, since that is
where eigenvector centrality concentrates, while the true center sits near the
path's midpoint. As the path grows, $D$ approaches $2D'$, matching the tight
general bound $D' \geq D(G)/2$ that holds for \emph{any} choice of root
\citep{hakimi1964}; \merw's hub does no better here than an arbitrary one. This
construction has a cut vertex on the path.

The bound below needs $h^\star$ to have enough neighbors that any other node is
reachable in a couple of hops, so we first show that being the highest
eigenvector centrality node already guarantees a minimum degree, with no extra
assumption on $G$.

\begin{proposition}[Hub degree lower bound]
\label{prop:hubdeg}
The hub's degree is at least the graph's spectral radius: $\deg(h^\star) \geq \lambda$.
\end{proposition}

\begin{theorem}[Dense graphs: depth at most 2]
\label{thm:mdst-gap}
Let $\delta_{\bar N}(h^\star) = \min_{u \notin \mathcal{N}_G(h^\star)\cup\{h^\star\}} \deg(u)$ be the smallest degree among the nodes $h^\star$ is not directly connected to. If $\deg(h^\star) + \delta_{\bar N}(h^\star) \geq N$, then $D \leq 2$.
\end{theorem}

\begin{proof}
Take any $u \neq h^\star$ with $u \notin \mathcal{N}_G(h^\star)$; every neighbor of $h^\star$ already has $d(h^\star,\cdot)=1$, so it suffices to show $d(h^\star,u)\leq2$ for such $u$. Both $\mathcal{N}_G(h^\star)$ and $\mathcal{N}_G(u)$ are subsets of $V\setminus\{h^\star,u\}$, and since $\deg(u)\geq\delta_{\bar N}(h^\star)$, their sizes satisfy $|\mathcal{N}_G(h^\star)|+|\mathcal{N}_G(u)| \geq \deg(h^\star)+\delta_{\bar N}(h^\star) \geq N$ by the hypothesis. Since $|V\setminus\{h^\star,u\}|=N-2 < N$, the two neighbor sets cannot be disjoint, so some $w$ is a common neighbor, giving $d(h^\star,u)\leq2$ via $h^\star\text{-}w\text{-}u$.
\end{proof}

Since $D' \geq 1$ on any connected graph with more than one node,
Theorem~\ref{thm:mdst-gap} says \merw's tree is at most one hop deeper than the
true MDST whenever this condition holds. By Proposition~\ref{prop:hubdeg}, the
hub's degree is at least $\lambda$, so the condition is easier to satisfy the
more concentrated $G$'s spectral radius is around $h^\star$. It requires only
$h^\star$ and the nodes outside its immediate neighborhood to be
well-connected, not every node in $G$.

\paragraph{Well-connected graphs.} Theorem~\ref{thm:mdst-gap}'s hypothesis is a strong density requirement. A weaker condition, the graph's algebraic connectivity $\lambda_2$ (the second-smallest Laplacian eigenvalue) bounded away from $0$, already bounds the gap logarithmically in $N$, with no density requirement on individual nodes.

\begin{theorem}[Depth gap under bounded algebraic connectivity]
\label{thm:mdst-gap-spectral}
Let $\lambda_2$ be the algebraic connectivity of $G$ (the second-smallest eigenvalue of the graph Laplacian $L = \mathrm{Diag}(\deg) - A$) and $\lambda_\infty$ its largest Laplacian eigenvalue. Then
\[
D - D' \leq \left\lceil \frac{\lambda_\infty + \lambda_2}{4\lambda_2} \ln(N-1) \right\rceil.
\]
\end{theorem}

Unlike Theorem~\ref{thm:mdst-gap}, this bound does not rely on $h^\star$'s own degree at all: it holds for the eccentricity of \emph{any} vertex, including $h^\star$, purely from the graph's global expansion. It grows with $N$ rather than staying constant, but it applies to a much wider class of graphs, including sparse ones, as long as they do not have a bottleneck that makes $\lambda_2$ small. A long path or cycle, whose algebraic connectivity vanishes as $N$ grows, is exactly the kind of graph excluded by this hypothesis, and is also where the worst-case behavior above is observed.

Theorem~\ref{thm:mdst-gap} already yields a high-probability guarantee on random graphs with no new proof: on an Erd\H{o}s--R\'{e}nyi graph $G(N,p)$ with $p$ bounded above $1/2$ by a constant margin, every node's degree concentrates tightly around its mean $(N-1)p$, so $\deg(h^\star)+\delta_{\bar N}(h^\star)$ exceeds $N$ with probability approaching 1 as $N$ grows. So on dense enough Erd\H{o}s--R\'{e}nyi graphs, the difference between $D$ and $D'$ is, with high probability, negligible: at most one hop.

\merw does not try to avoid the worst case directly: doing so would require the same all-pairs distance information that exact MDST construction depends on (Section~\ref{sec:related}), which is precisely the global knowledge \merw is designed to do without.

\subsection{Instantiation: Regret Minimization}
\label{sec:regret}

With $K$ arms of unknown means $\mu_1 \geq \cdots \geq \mu_K$ and sub-Gaussian rewards, the objective is to minimize $R(T) = \sum_{i}\sum_{t=1}^T \mathbb{E}[\mu_1 - \mu_{a_i(t)}]$. Each agent's local contribution $b_i$ is its new per-arm pull counts and reward sums since the last cycle; the hub's decision rule pulls the UCB1-maximizing arm \citep{auer2002} on the aggregate and broadcasts the running total $(\hat n, \hat s, P)$, where the total-pull count $P$ (following \citep{martinez2019decentralized}) lets every node use a consistent UCB bonus. Each relay cycle lasts $2D+1$ rounds: one pull round, $D$ uplink rounds, one hub-decision round, $D$ downlink rounds.

\begin{corollary}[Regret matches centralized UCB]
\label{cor:regret}
Under the \merw relay protocol, the leading term of the expected group cumulative regret matches a centralized UCB agent on $P$ total pulls, with $P = \Theta(NT/(2D+1))$.
\end{corollary}

\subsection{Instantiation: Best Arm Identification}
\label{sec:bai}

In BAI, given confidence $\delta$, the goal is to identify $a^\star = \arg\max_k \mu_k$ with probability $\geq 1-\delta$ using as few total pulls as possible. \merw-BAI adapts Hillel's Algorithm 3 \citep{hillel2013distributed} to the same generic protocol: each agent's local contribution $b_i$ is its per-arm sums over the current surviving arm set $S_{r-1}$, pulled $L_r = \lceil t_r - t_{r-1} \rceil$ times each round, where $t_r = \frac{2}{N\epsilon_r^2}\ln\frac{4Kr^2}{\delta}$ is Hillel's schedule and $\epsilon_r = 2^{-r}$. Every node computes $L_r$ independently from this formula, so no per-round negotiation is needed, but $N$ appears in the formula itself, so every node must know $N$ in advance: if two nodes used different values of $N$ they would compute different $L_r$ and desynchronize. This is unlike \merw-UCB, whose UCB bonus depends only on the total-pull count $P$ as it is actually observed through the downlink, never on $N$ directly. In principle $N$ could be learned the same way the tree height $D$ is, by having the hub count distinct senders on an uplink and broadcast the result down; but $D$ is discovered as a free byproduct of the first ordinary cycle, whereas the very first $L_1$ is needed \emph{before} any uplink has happened, so learning $N$ this way would require a dedicated bootstrap cycle up front. We treat $N$ as known in advance and leave decentralized bootstrap for future work. The hub's decision rule computes global averages from the aggregate, eliminates arms falling below the current threshold $\epsilon_r$, and broadcasts the surviving set $S_r$ instead of a running statistic. The hub, not any other node, determines when to stop, once $|S_r|=1$, and this outcome is broadcast on the downlink like any other decision. Consequently \merw-BAI's round length $L_r + 2D$ shrinks across elimination rounds as $\epsilon_r$ tightens, unlike \merw-UCB's fixed $2D+1$.

\begin{corollary}[BAI sample complexity matches Hillel]
\label{cor:bai}
The total number of arm pulls required by \merw-BAI to identify $a^\star$ with probability $\geq 1-\delta$ is identical to Hillel's algorithm; the only overhead is $2D$ extra time steps per elimination round.
\end{corollary}


\begin{table}[htbp]
\caption{Function behavior across different algorithm instantiations.}
\label{tab:instantiations}
\small
\renewcommand{\arraystretch}{1.3} % Adds breathing room between rows
\begin{tabularx}{\columnwidth}{llY}
\toprule
& \textbf{Function} & \textbf{Description / Behavior} \\
\midrule
% --- FIRST INSTANTIATION ---
\textbf{\merw-UCB} & \textsc{Init} 			& pull each $k\in\{1,..,K\}$ once, $K$ rounds \\
			       & \textsc{Local-Pull}    & UCB-maximizing arm, 1 round \\
                   & \textsc{Hub-Decide}    & UCB-pull on $B$; never stops early \\
                   & \textsc{Downlink}      & aggregate $(\hat n,\hat s,P)$ \\
\midrule
% --- SECOND INSTANTIATION ---
\textbf{\merw-BAI} & \textsc{Init}       & $\sigma_i \leftarrow S_0$, 0 rounds \\
			       & \textsc{Local-Pull} & every $k\in S_{r-1}$, $L_r$ times \\
                   & \textsc{Hub-Decide} & eliminate below $\epsilon_r$; stop when $|S_r|{=}1$ \\
                   & \textsc{Downlink}   & surviving set $S_r$ \\
\bottomrule
\end{tabularx}
\end{table}


\section{Fault Tolerance}
\label{sec:ft}

A centralized coordinator has one obvious weakness: if it fails, the system
fails with it, since every agent's information passes through that single
point. The \merw hub is a single node running the decision rule on globally
aggregated statistics, so it carries the same weakness. \merwft removes it:
any single node can fail, hub or not, without the network losing its ability
to coordinate.

Take any node in $\mathcal{T}$. If it fails, \merwft must handle it, and the
hub and a non-hub node fail very differently. The hub alone holds the
decision rule and the aggregated statistics; lose it, and no downlink goes
out, no agent learns the round's outcome, and the whole coordination
structure stops. A non-hub node only relays one subtree; lose it, and only
the agents below it are affected, while the hub and the rest of the tree
keep running. We cover hub failure first, since it is the more consequential
case.

\subsection{Hub Failure}

\merwft tolerates permanent hub failure at no extra cost during normal
operation. The hub's direct children already receive every downlink
broadcast, so they already hold a copy of the hub's global state. We use
this: the child with the highest eigenvector centrality, $b^\star =
\arg\max_{j \in \mathrm{ch}(h^\star)} \tilde\psi_j$, becomes the backup. The
hub announces this choice once, in the first downlink after it learns its
children.

\begin{proposition}[Backup staleness]
\label{prop:staleness}
At the end of any cycle, $b^\star$'s stored state equals the hub's state after the hub's own pull that cycle. If the hub fails during the next cycle, the backup's state is at most one full cycle stale.
\end{proposition}

$b^\star$ detects hub silence by timeout, then does four things. First, it
self-promotes: it takes the root role, using its own stored state. Second,
it floods a promotion message over graph edges, not tree edges, since
$b^\star$ shares no tree edge with its former siblings; this tells them to
re-route through $b^\star$. Third, it recomputes and broadcasts the new tree
height $D_{\mathrm{new}}$. Fourth, it resumes the protocol right away. This
only works if the graph is 2-connected: that is what guarantees a former
sibling still has a path to $b^\star$ once the hub is gone.

\begin{proposition}[Density implies 2-connectivity]
\label{prop:dense-2conn}
If $\delta(G) \geq N/2$, where $\delta(G)$ is the minimum degree over all nodes, then $G$ is 2-connected.
\end{proposition}

\begin{proposition}[Failover correctness and completeness]
\label{prop:ft-correct}
After promotion, the network forms a valid spanning tree rooted at $b^\star$ with height $D_{\mathrm{new}} \leq D+1$ and no cycles. The procedure completes in $O(\mathrm{diam}(G))$ rounds in general, and $O(1)$ rounds when $b^\star$ is directly connected to all former siblings.
\end{proposition}

Re-routing can take several rounds, and this creates a problem: a sibling
that is itself an internal node has not yet forwarded a downlink to its own
subtree. Left alone, its descendants would time out waiting for a parent
that is mid-reroute, and each would wrongly declare a non-hub failure. We
add a lightweight \textsc{Hold} signal to fix this. It piggybacks on the
existing downlink mechanism: a re-routing sibling forwards \textsc{Hold} to
its subtree right away, and this suspends timeouts there until re-routing
finishes.

\begin{proposition}[No false timeouts during re-routing]
\label{prop:noholdfalsepositive}
Under the \textsc{Hold} propagation rule above, no node in a re-routing sibling's subtree declares its parent failed while the sibling re-routing step of promotion is still in progress, provided that step completes within $\mathrm{diam}(G)$ rounds of the sibling receiving the flood.
\end{proposition}

Backup selection and promotion are the same procedure, run again. A
promoted hub re-runs backup selection to name its own successor, and this
lets \merwft survive any finite sequence of hub failures, one at a time.

\begin{proposition}[Sequential hub failure tolerance]
\label{prop:recursive}
Under the recursive backup protocol, \merwft tolerates any finite sequence of hub failures $h_0=h^\star, h_1, h_2,\ldots$, where each $h_{k+1}$ is the backup selected by $h_k$. After each failure, exactly one cycle runs without a backup, and the protocol resumes with a newly selected successor.
\end{proposition}

\subsection{Non-Hub Failure}

A non-hub node $v$ at depth $d_v$ only affects its own subtree when it
fails; the rest of the tree is unaffected. $v$'s parent is the one who
notices: it is the fastest possible point of detection, since $v$'s children
would need $D-d_v$ hops to notice, and the hub a full cycle. We do not want
to scan neighbors at failure time, so instead, once depths are set after the
first cycle, every node precomputes a \emph{swap parent}. This follows the
best-swap-edge framework of Nardelli et al.\ \citep{nardelli2003}, and it
minimizes the largest depth increase across the node's subtree. Once a swap
parent is known, re-attachment takes exactly one round.

\begin{proposition}[Non-hub failover correctness]
\label{prop:nonhub-correct}
After re-attachment of all orphaned children of a failed node $v$, the remaining live nodes form a valid spanning tree with height $D_{\mathrm{new}} \leq D + |\mathrm{ch}(v)|$ and no cycles.
\end{proposition}

\subsection{Recovery}

So far we assumed a failed node stays down. The general version of \merwft
also handles a node coming back. A recovering node $v$ has no reliable
state: it may have missed any number of promotions and re-routings while it
was down. So it does not try to recover its old position in $\mathcal{T}$;
it rejoins as if it were new. $v$ sends a \textsc{Ping} to each graph
neighbor. Any live neighbor $j$ that is already attached replies with its
current depth $d_j$, and $v$ attaches to whichever neighbor gives the
smallest $d_j$, breaking ties by smallest identity, as in the initial
max-flooding. It sets $d_v = d_j + 1$ and waits for the next downlink
through $j$ to fill in its mirrored state $(\hat n, \hat s)$, the same way
any node does at a cycle boundary.

\begin{proposition}[Recovery correctness]
\label{prop:recovery-correct}
If $v$ attaches to a live, already-attached neighbor $j$ upon recovery, the resulting structure remains a valid spanning tree over all currently live nodes, with no cycles introduced, regardless of which node currently serves as hub.
\end{proposition}

This works the same way even under repeated hub failure. If $v$ happens to
reconnect through the current backup, or through a hub that was promoted
earlier, nothing changes: recovery only needs $j$ to be live and attached,
not any particular identity for $j$.

\section{Experiments}
\label{sec:experiments}

We evaluate both instantiations on Erd\H{o}s--R\'{e}nyi (ER) graphs. Here
eigenvector centrality is spread across several nodes, but it still
identifies a clear hub (Figure~\ref{fig:merw_viz_er}).

For regret minimization ($N=100$, $K=5$, $T=5000$, $p=0.5$, 50 runs;
Figure~\ref{fig:regret_er}), the hub, identified without any central authority,
coordinates the network to logarithmic group regret while non-hub agents
accumulate no regret during communication rounds. For best arm identification
($K=10$, $\delta=0.05$, $p=0.5$, 50 runs per $N$), Figure~\ref{fig:bai_er}
compares total arm pulls against Hillel's centralized algorithm as a function
of $N$, using the same graph and MERW-identified hub for both.

\begin{figure*}[t]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=0.85\columnwidth]{results/Regret/relay_regret_er_N100_K5_T5000.pdf}
        \caption{Cumulative regret on an Erd\H{o}s--R\'{e}nyi graph ($N=100$, $K=5$, $T=5000$, $p=0.5$, 50 runs). \emph{Top}: group regret vs.\ round. \emph{Bottom}: group regret vs.\ total arm pulls. Shaded bands are standard error of the mean.}
        \label{fig:regret_er}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=0.85\columnwidth]{results/BAI/merw_ucb_bai_er_K10.pdf}
        \caption{BAI sample complexity on Erd\H{o}s--R\'{e}nyi graphs ($K=10$, $\delta=0.05$, $p=0.5$, 50 runs per $N$). \emph{Top}: total arm pulls vs.\ $N$. \emph{Bottom}: pulls per agent vs.\ $N$. Error bars are standard error of the mean.}
        \label{fig:bai_er}
    \end{minipage}
\end{figure*}

% \FloatBarrier

To test the failover mechanism of Section~\ref{sec:ft} directly, we run
\merwft on an ER graph ($N=20$, $p=0.5$, $K=10$, $T=2000$) and kill the hub
at $t=500,1000,1500$. Each time, the backup promotes itself and the network
recovers. Figure~\ref{fig:ft_regret} shows the resulting group regret: every
failure is followed by a recovery within a handful of rounds, and the
regret curve afterward grows at the same rate as before the failure, with
no lasting penalty. Figure~\ref{fig:ft_trees} shows the induced routing tree
on the underlying graph right after each failure. The backup correctly
re-roots the tree and brings back every former sibling of the failed hub.

\begin{figure}[tb]
    \centering
    \includegraphics[width=0.95\columnwidth]{results/FaultTolerance/fault_tolerance_er_N20_K10_T2000_at500-1000-1500.pdf}
    \caption{Group regret for \merwft on an Erd\H{o}s--R\'{e}nyi graph ($N=20$, $p=0.5$, $K=10$, $T=2000$) with the hub killed at $t=500,1000,1500$. Dashed lines mark failures, dotted lines mark recovery. All three failures are recovered from.}
    \label{fig:ft_regret}
\end{figure}

\begin{figure}[tb]
    \centering
    \includegraphics[width=0.95\columnwidth]{results/FaultTolerance/fault_tolerance_trees_er_N20_K10_T2000_at500-1000-1500.pdf}
    \caption{Underlying graph and induced routing tree: initial, and immediately after each of the three hub failures. Faded edges are non-tree graph edges; arrows point from child to parent; dead nodes are shown in gray.}
    \label{fig:ft_trees}
\end{figure}

\section{Discussion}
\label{sec:discussion}

Section~\ref{sec:mdst-gap} shows that the gap between $D$ and $D'$ is small
on dense or well-expanding graphs. A complete relation that also covers
sparser graphs and other random graph models is left for future work.

The graph must be connected and undirected, and stays that way except for
the node failures \merwft handles. $D$ can be large on sparse or path-like
graphs; this is exactly the regime Section~\ref{sec:mdst-gap} identifies.
BAI needs agents to know $N$ in advance, to compute Hillel's pull schedule;
regret minimization needs no such thing, since it learns everything from
the protocol itself. \merwft only covers fail-stop failures: a node works
correctly, goes silent, or comes back and rejoins through the recovery
mechanism of Section~\ref{sec:ft}. It has no mechanism for a node that stays
active but sends incorrect messages, and none for the hub and its backup
failing at once. Both are left for future work.

\section{Conclusion}
\label{sec:conclusion}

A coordinator can emerge from purely local computation: no node ever needs
to see the graph's global structure, or even know how many other agents
there are. \merw builds this coordinator from eigenvector centrality alone,
using only local gossip and a short flooding phase, and the resulting hub
is statistically sufficient for any decision rule that depends only on
globally aggregated statistics. We instantiated this twice, for regret
minimization and for best arm identification, and both match centralized
performance up to a bounded, $O(D)$ communication overhead per round.

Any single node, hub or not, can still fail. \merwft handles this: it
removes the failure points a tree creates at zero or near-zero steady-state
cost, and it brings a returning node back into the tree without disrupting
anything else. What is left, as Section~\ref{sec:discussion} lays out, is
mostly about tightening guarantees already in place: a general bound
relating $D$ to the true minimum diameter, and a fault model that goes
beyond fail-stop failures.

\bibliography{references}

\end{document}
